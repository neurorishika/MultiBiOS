# Short validation config for live FicTrac experiment-path checks.
# Base FicTrac settings now come from config/hardware.yaml. Keep any FicTrac
# keys here only as temporary deprecated overrides during migration.

teensy_port: "COM4"
teensy_baud: 115200

other_camera_exposure_us: 4500
other_camera_roi_width: 400
other_camera_roi_height: 400

mfc_mode: "none"

latch_interval_ms: 10.0

data_dir: "data/runs"
open_explorer: false
explorer_port: 8050
mfc_live_interval_s: 0.0
