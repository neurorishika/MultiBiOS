# Short validation config for live FicTrac experiment-path checks.
# All rig and camera-recording settings now come from config/hardware.yaml.
# Keep values here only as temporary deprecated overrides during migration.

mfc_mode: "none"

data_dir: "data/runs"
open_explorer: false
explorer_port: 8050
mfc_live_interval_s: 0.0
